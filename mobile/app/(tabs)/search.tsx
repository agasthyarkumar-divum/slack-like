import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function SearchScreen() {
  return (
    <PlaceholderScreen
      title="Search"
      subtitle="Messages / Users / Channels / Files tabs land in Phase 8 (architecture.md §6)."
    />
  );
}
