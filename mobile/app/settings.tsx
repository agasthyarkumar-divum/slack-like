import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      subtitle="Dark mode, notification prefs, privacy, account land in Phase 10 (architecture.md §12)."
    />
  );
}
