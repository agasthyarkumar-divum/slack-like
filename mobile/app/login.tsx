import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Login"
      subtitle="Email/password sign-in lands in Phase 3 (architecture.md §7)."
    />
  );
}
